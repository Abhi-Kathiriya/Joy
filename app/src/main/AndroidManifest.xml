<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.joy">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application xmlns:tools="http://schemas.android.com/tools"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Joy"
        tools:ignore="MissingClass"
        tools:replace="android:theme">
        <activity android:name=".activities.SuccessActivity"></activity>
        <activity android:name=".activities.CartActivity" />
        <activity android:name=".activities.ProductDetailsActivity" />
        <activity android:name=".activities.OffersActivity" />
        <activity android:name=".activities.ShopProductActivity" />
        <activity android:name=".activities.ShopDetailsActivity" />
        <activity android:name=".activities.EditProfileUserActivity" />
        <activity android:name=".activities.ForgotPasswordActivity" />
        <activity android:name=".activities.MainUserActivity" />
        <activity android:name=".activities.EditProductSellerActivity" />
        <activity android:name=".activities.EditProfileSellerActivity" />
        <activity android:name=".activities.AddItemActivity" />
        <activity android:name=".activities.AddProductActivity" />
        <activity
            android:name=".activities.MainSellerActivity"
            android:label="@string/title_activity_main_seller"
            android:theme="@style/Theme.Joy.NoActionBar" />
        <activity android:name=".activities.RegisterSellerActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterUserActivity" />
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>